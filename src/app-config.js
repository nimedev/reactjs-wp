/**
 * Config constants for the application.
 * @module app-config
 */

// API base url
export const REST_URL = process.env.REACTJS_API_URL

// Save the environment
export const ENV = process.env.NODE_ENV
